# modules-common

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build modules-common` to build the library.

## Running unit tests

Run `nx test modules-common` to execute the unit tests via [Jest](https://jestjs.io).
